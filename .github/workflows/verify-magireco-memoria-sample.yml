name: Diagnose Legacy Memoria Sample

on:
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: |
          echo 'This legacy single-pass workflow is intentionally manual-only.'
          echo 'Random source HTTP 522 responses made one-pass coverage nondeterministic.'
          echo 'Use Verify Memoria Multipass Sample for catalog reliability validation.'
