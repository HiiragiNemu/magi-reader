name: Test Magireco Reader v6.4 Audio

on:
  pull_request:
    branches: [main]
    paths:
      - 'wiki-preservation/static/**'
      - 'wiki-preservation/tests/production-reader-v6.mjs'
      - 'wiki-preservation/tests/production-audio-v64.mjs'
      - 'wiki-preservation/scripts/**'
      - '.github/workflows/deploy-magireco-ui-hotfix.yml'
      - '.github/workflows/test-magireco-reader-v6.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: magireco-reader-v6-production-browser
  cancel-in-progress: true

jobs:
  production-browser:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    env:
      READER_REVISION: '6.4'
    steps:
      - name: Check out v6.4 production tests
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            wiki-preservation/tests/production-reader-v6.mjs
            wiki-preservation/tests/production-audio-v64.mjs
          sparse-checkout-cone-mode: true
          show-progress: false

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Playwright and Chromium
        run: |
          npm init -y
          npm install --no-save playwright
          npx playwright install --with-deps chromium

      - name: Wait for Reader v6.4 production revision and audio corpus
        shell: bash
        run: |
          set -euo pipefail
          for attempt in $(seq 1 100); do
            if curl --fail --silent --show-error --location \
              --header 'Cache-Control: no-cache' \
              "https://magireco-cn-reader.pages.dev/health.json?revision=${READER_REVISION}-${GITHUB_RUN_ID}-$attempt" \
              -o /tmp/health.json; then
              if python - <<'PY'
          import json
          import os
          from pathlib import Path
          value = json.loads(Path('/tmp/health.json').read_text(encoding='utf-8-sig'))
          ok = (
              value.get('uiRevision') == os.environ['READER_REVISION']
              and value.get('counts', {}).get('audio', 0) >= 18000
              and value.get('voiceAudio', {}).get('r2') is False
          )
          raise SystemExit(0 if ok else 1)
          PY
              then
                exit 0
              fi
            fi
            sleep 6
          done
          echo "Reader ${READER_REVISION} audio build did not propagate before browser validation." >&2
          exit 1

      - name: Prepare current production regression
        run: |
          python - <<'PY'
          import os
          from pathlib import Path
          source = Path('wiki-preservation/tests/production-reader-v6.mjs')
          target = source.with_name('production-reader-current.mjs')
          text = source.read_text(encoding='utf-8')
          old = "const revision = '6.0';"
          assert text.count(old) == 1
          revision = os.environ['READER_REVISION']
          target.write_text(text.replace(old, f"const revision = '{revision}';", 1), encoding='utf-8')
          PY

      - name: Run Reader production regression
        run: node wiki-preservation/tests/production-reader-current.mjs

      - name: Run full voice audio production regression
        run: node wiki-preservation/tests/production-audio-v64.mjs

      - name: Require clean offline core-index startup
        run: |
          python - <<'PY'
          import json
          import os
          from pathlib import Path
          result = json.loads(Path('reader-v6-evidence/result.json').read_text(encoding='utf-8'))
          forbidden = ('/data/archive-index.json', '/data/category-index.json', '/data/portal-index.json')
          failures = [
              item for item in result.get('events', [])
              if item.get('type') == 'requestfailed'
              and any(value in item.get('url', '') for value in forbidden)
          ]
          assert not failures, failures
          registration = result.get('registration', {})
          expected = f"/sw-v{os.environ['READER_REVISION']}.js"
          assert expected in registration.get('active', ''), registration
          audio = json.loads(Path('reader-audio-evidence/result.json').read_text(encoding='utf-8'))
          assert audio['manifest']['voiceFiles'] >= 18000
          assert audio['detailMetrics']['readyState'] >= 1
          PY

      - name: Upload production evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: magireco-reader-v64-audio-production-evidence
          path: |
            reader-v6-evidence
            reader-audio-evidence
          if-no-files-found: warn
          retention-days: 14
