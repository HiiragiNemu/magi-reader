name: Legacy Magireco Reader v5 Test

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-note:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Reader v5 browser regression has been superseded by Reader v6.'
