name: Apply main-only V30 font scope

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired-bootstrap:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded by apply-main-only-v30-v2.yml'
